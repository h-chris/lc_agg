<%= render 'layouts/header' %>

<div class="content">
  <%= paginate @youtubes %>

  <% @youtubes.each do |vid| %>
    <div class="yt_title">
      <%= vid.title %>
    </div>

    <%= render 'layouts/youtube', locals = { embed: vid.embed_id } %>

    <div class="separator"></div>
  <% end %>

  <%= paginate @youtubes %>
</div>

<%= render 'layouts/footer' %>
